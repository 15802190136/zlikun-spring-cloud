spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/zlikun/zlikun-spring-cloud-config
          search-paths: example/user
          username: zlikun
          password: