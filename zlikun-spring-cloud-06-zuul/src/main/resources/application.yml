server:
  port: 8000
eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/
spring:
  application:
    name: microservice-gateway-zuul
management:
  security:
    enabled: false